
"use strict";

catName("Chloe");

function catName(name) {
  console.log("My cat's name is " + name);
}
// Prints the console ouptut even if the function body is written after function call;
console.log(num); // Executes but shows undefined as value is not assigned;
var num = 6; // Initialization

b = 1; // initialization.
let b; // Throws ReferenceError: Cannot access 'b' before initialization

a = 1; // initialization.
const a; // Throws SyntaxError: Missing initializer in const declaration

